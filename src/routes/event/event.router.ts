// import { EventController } from '../../controllers/event/event.controller';
import { Router } from 'express';

const eventRouter = Router();

// eventRouter.post('/booking', EventController.create);

export { eventRouter };